@page "/characterbuilder"
@using DndInator.Services
@using DndShared.Models
@using DndInator.Components
@using Microsoft.AspNetCore.Components.Forms
@inject ISpeciesService SpeciesService
@inject IClassService ClassService
@inject IBackgroundService BackgroundService
@inject IFeatService FeatService
@inject ISpellService SpellService
@inject IWeaponService WeaponService
@inject IArmorService ArmorService
@inject IAdventuringGearService AdventuringGearService
@inject IMagicItemService MagicItemService
@inject IToolService ToolService
@inject IDiceService DiceService
@inject ICharacterSheetService CharacterSheetService
@inject IStorageService StorageService
@inject NavigationManager NavigationManager

<PageTitle>Character Builder</PageTitle>

<div class="container-fluid py-4">
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="display-4 text-primary mb-2">
                <i class="bi bi-person-plus-fill me-3"></i>
                Character Builder
            </h1>
            <p class="lead text-muted">Create your D&D 5e character step by step</p>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <!-- Tab Navigation -->
            <ul class="nav nav-pills nav-fill mb-4 character-tabs" id="characterBuilderTabs" role="tablist">
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == 0 ? "active" : "")" 
                            @onclick="() => SetActiveTab(0)" 
                            type="button">
                        <i class="bi bi-1-circle-fill d-block fs-4 mb-1"></i>
                        Basic Info
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == 1 ? "active" : "")" 
                            @onclick="() => SetActiveTab(1)" 
                            type="button">
                        <i class="bi bi-2-circle-fill d-block fs-4 mb-1"></i>
                        Race
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == 2 ? "active" : "")" 
                            @onclick="() => SetActiveTab(2)" 
                            type="button">
                        <i class="bi bi-3-circle-fill d-block fs-4 mb-1"></i>
                        Class
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == 3 ? "active" : "")" 
                            @onclick="() => SetActiveTab(3)" 
                            type="button">
                        <i class="bi bi-4-circle-fill d-block fs-4 mb-1"></i>
                        Background
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == 4 ? "active" : "")" 
                            @onclick="() => SetActiveTab(4)" 
                            type="button">
                        <i class="bi bi-5-circle-fill d-block fs-4 mb-1"></i>
                        Ability Scores
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == 5 ? "active" : "")" 
                            @onclick="() => SetActiveTab(5)" 
                            type="button">
                        <i class="bi bi-6-circle-fill d-block fs-4 mb-1"></i>
                        Skills
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == 6 ? "active" : "")" 
                            @onclick="() => SetActiveTab(6)" 
                            type="button">
                        <i class="bi bi-6-circle-fill d-block fs-4 mb-1"></i>
                        Feats
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == 7 ? "active" : "")" 
                            @onclick="() => SetActiveTab(7)" 
                            type="button">
                        <i class="bi bi-7-circle-fill d-block fs-4 mb-1"></i>
                        Spells
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == 8 ? "active" : "")" 
                            @onclick="() => SetActiveTab(8)" 
                            type="button">
                        <i class="bi bi-8-circle-fill d-block fs-4 mb-1"></i>
                        Equipment
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link @(activeTab == 9 ? "active" : "")" 
                            @onclick="() => SetActiveTab(9)" 
                            type="button">
                        <i class="bi bi-9-circle-fill d-block fs-4 mb-1"></i>
                        Preview & Export
                    </button>
                </li>
            </ul>

            <!-- Tab Content -->
            <div class="tab-content">
                <!-- Tab 0: Basic Info -->
                @if (activeTab == 0)
                {
                    <div class="card shadow-lg border-0 animate-slide-in">
                        <div class="card-header bg-gradient-primary text-white">
                            <h4 class="mb-0"><i class="bi bi-person-vcard me-2"></i>Basic Character Information</h4>
                        </div>
                        <div class="card-body p-4">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Character Name <span class="text-danger">*</span></label>
                                    <input type="text" class="form-control form-control-lg" 
                                           @bind="characterName" 
                                           placeholder="Enter your character's name" />
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Alignment</label>
                                    <select class="form-select form-select-lg" @bind="selectedAlignment">
                                        @foreach (CharacterAlignment alignment in Enum.GetValues(typeof(CharacterAlignment)))
                                        {
                                            <option value="@alignment">@alignment.ToString()</option>
                                        }
                                    </select>
                                </div>
                                <div class="col-md-3">
                                    <label class="form-label fw-bold">Level</label>
                                    <input type="number" class="form-control form-control-lg" 
                                           @bind="characterLevel" 
                                           min="1" max="20"
                                           placeholder="1" />
                                </div>
                                <div class="col-md-3">
                                    <label class="form-label fw-bold">Experience Points</label>
                                    <input type="number" class="form-control form-control-lg" 
                                           @bind="characterExperience" 
                                           min="0"
                                           placeholder="0" />
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Character Portrait (Optional)</label>
                                    <InputFile OnChange="HandlePortraitUpload" class="form-control" accept="image/*" />
                                    @if (!string.IsNullOrEmpty(characterPortrait))
                                    {
                                        <div class="mt-2">
                                            <img src="@characterPortrait" alt="Character Portrait" style="max-width: 150px; max-height: 150px; border-radius: 8px;" />
                                            <button class="btn btn-sm btn-outline-danger ms-2" @onclick="ClearPortrait">
                                                <i class="bi bi-x-circle"></i> Remove
                                            </button>
                                        </div>
                                    }
                                </div>
                                <div class="col-12">
                                    <label class="form-label fw-bold">Character Description (Optional)</label>
                                    <textarea class="form-control" rows="3" 
                                              @bind="characterDescription" 
                                              placeholder="Describe your character's appearance, personality, or backstory..."></textarea>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer bg-light">
                            <button class="btn btn-primary btn-lg float-end" @onclick="() => SetActiveTab(1)">
                                Next: Choose Race <i class="bi bi-arrow-right ms-2"></i>
                            </button>
                        </div>
                    </div>
                }

                <!-- Tab 1: Race Selection -->
                @if (activeTab == 1)
                {
                    <div class="card shadow-lg border-0 animate-slide-in">
                        <div class="card-header bg-gradient-success text-white">
                            <h4 class="mb-0"><i class="bi bi-emoji-smile me-2"></i>Choose Your Race</h4>
                        </div>
                        <div class="card-body p-4">
                            @if (availableSpecies == null)
                            {
                                <div class="text-center py-5">
                                    <div class="spinner-border text-primary" role="status">
                                        <span class="visually-hidden">Loading...</span>
                                    </div>
                                    <p class="mt-3 text-muted">Loading races...</p>
                                </div>
                            }
                            else
                            {
                                <div class="row g-3">
                                    @foreach (var species in availableSpecies.Take(12))
                                    {
                                        <div class="col-md-4 col-lg-3">
                                            <div class="card h-100 hover-card @(selectedSpecies?.Name == species.Name ? "border-success border-3" : "")" 
                                                 @onclick="() => SelectSpecies(species)"
                                                 style="cursor: pointer;">
                                                <div class="card-body">
                                                    <h6 class="card-title fw-bold">@species.Name</h6>
                                                    <small class="text-muted">
                                                        <div><strong>Size:</strong> @species.Size</div>
                                                        <div><strong>Speed:</strong> @species.Speed</div>
                                                    </small>
                                                    @if (selectedSpecies?.Name == species.Name)
                                                    {
                                                        <div class="mt-2">
                                                            <span class="badge bg-success">
                                                                <i class="bi bi-check-circle-fill"></i> Selected
                                                            </span>
                                                        </div>
                                                    }
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>
                                
                                @if (selectedSpecies != null)
                                {
                                    <div class="alert alert-success mt-4">
                                        <h5 class="alert-heading">@selectedSpecies.Name</h5>
                                        <p class="mb-0"><strong>Traits:</strong></p>
                                        <ul>
                                            @foreach (var trait in selectedSpecies.Traits ?? new List<string>())
                                            {
                                                <li>@trait</li>
                                            }
                                        </ul>
                                    </div>
                                }
                            }
                        </div>
                        <div class="card-footer bg-light">
                            <button class="btn btn-secondary btn-lg" @onclick="() => SetActiveTab(0)">
                                <i class="bi bi-arrow-left me-2"></i> Back
                            </button>
                            <button class="btn btn-primary btn-lg float-end" 
                                    @onclick="() => SetActiveTab(2)"
                                    disabled="@(selectedSpecies == null)">
                                Next: Choose Class <i class="bi bi-arrow-right ms-2"></i>
                            </button>
                        </div>
                    </div>
                }

                <!-- Tab 2: Class Selection -->
                @if (activeTab == 2)
                {
                    <div class="card shadow-lg border-0 animate-slide-in">
                        <div class="card-header bg-gradient-warning text-dark">
                            <h4 class="mb-0"><i class="bi bi-shield-fill-check me-2"></i>Choose Your Class</h4>
                        </div>
                        <div class="card-body p-4">
                            @if (availableClasses == null)
                            {
                                <div class="text-center py-5">
                                    <div class="spinner-border text-primary" role="status">
                                        <span class="visually-hidden">Loading...</span>
                                    </div>
                                    <p class="mt-3 text-muted">Loading classes...</p>
                                </div>
                            }
                            else
                            {
                                <div class="row g-3">
                                    @foreach (var classOption in availableClasses.Take(12))
                                    {
                                        <div class="col-md-6 col-lg-4">
                                            <div class="card h-100 hover-card @(selectedClass?.Name == classOption.Name ? "border-warning border-3" : "")" 
                                                 @onclick="() => SelectClass(classOption)"
                                                 style="cursor: pointer;">
                                                <div class="card-body">
                                                    <h6 class="card-title fw-bold">@classOption.Name</h6>
                                                    <small class="text-muted">
                                                        <div><strong>Hit Die:</strong> @classOption.HitPointDie</div>
                                                        <div><strong>Primary:</strong> @classOption.PrimaryAbility</div>
                                                    </small>
                                                    @if (selectedClass?.Name == classOption.Name)
                                                    {
                                                        <div class="mt-2">
                                                            <span class="badge bg-warning text-dark">
                                                                <i class="bi bi-check-circle-fill"></i> Selected
                                                            </span>
                                                        </div>
                                                    }
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>
                                
                                @if (selectedClass != null)
                                {
                                    <div class="alert alert-warning mt-4">
                                        <h5 class="alert-heading">@selectedClass.Name</h5>
                                        <p><strong>Saving Throws:</strong> @selectedClass.SavingThrowProficiencies</p>
                                        <p class="mb-0"><strong>Skills:</strong> @selectedClass.SkillProficiencies</p>
                                    </div>
                                }
                            }
                        </div>
                        <div class="card-footer bg-light">
                            <button class="btn btn-secondary btn-lg" @onclick="() => SetActiveTab(1)">
                                <i class="bi bi-arrow-left me-2"></i> Back
                            </button>
                            <button class="btn btn-primary btn-lg float-end" 
                                    @onclick="() => SetActiveTab(3)"
                                    disabled="@(selectedClass == null)">
                                Next: Choose Background <i class="bi bi-arrow-right ms-2"></i>
                            </button>
                        </div>
                    </div>
                }

                <!-- Tab 3: Background Selection -->
                @if (activeTab == 3)
                {
                    <div class="card shadow-lg border-0 animate-slide-in">
                        <div class="card-header bg-gradient-info text-white">
                            <h4 class="mb-0"><i class="bi bi-book me-2"></i>Choose Your Background</h4>
                        </div>
                        <div class="card-body p-4">
                            @if (availableBackgrounds == null)
                            {
                                <div class="text-center py-5">
                                    <div class="spinner-border text-primary" role="status">
                                        <span class="visually-hidden">Loading...</span>
                                    </div>
                                    <p class="mt-3 text-muted">Loading backgrounds...</p>
                                </div>
                            }
                            else
                            {
                                <div class="row g-3">
                                    @foreach (var background in availableBackgrounds.Take(12))
                                    {
                                        <div class="col-md-6">
                                            <div class="card h-100 hover-card @(selectedBackground?.Name == background.Name ? "border-info border-3" : "")" 
                                                 @onclick="() => SelectBackground(background)"
                                                 style="cursor: pointer;">
                                                <div class="card-body">
                                                    <h6 class="card-title fw-bold">@background.Name</h6>
                                                    <p class="card-text small text-muted">@background.Description</p>
                                                    @if (selectedBackground?.Name == background.Name)
                                                    {
                                                        <span class="badge bg-info">
                                                            <i class="bi bi-check-circle-fill"></i> Selected
                                                        </span>
                                                    }
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>
                            }
                        </div>
                        <div class="card-footer bg-light">
                            <button class="btn btn-secondary btn-lg" @onclick="() => SetActiveTab(2)">
                                <i class="bi bi-arrow-left me-2"></i> Back
                            </button>
                            <button class="btn btn-primary btn-lg float-end" 
                                    @onclick="() => SetActiveTab(4)"
                                    disabled="@(selectedBackground == null)">
                                Next: Ability Scores <i class="bi bi-arrow-right ms-2"></i>
                            </button>
                        </div>
                    </div>
                }

                <!-- Tab 4: Ability Scores -->
                @if (activeTab == 4)
                {
                    <div class="card shadow-lg border-0 animate-slide-in">
                        <div class="card-header bg-gradient-danger text-white">
                            <h4 class="mb-0"><i class="bi bi-lightning-fill me-2"></i>Roll Ability Scores</h4>
                        </div>
                        <div class="card-body p-4">
                            <div class="mb-4">
                                <h5>Choose a method:</h5>
                                <div class="btn-group w-100" role="group">
                                    <button class="btn btn-outline-primary @(abilityScoreMethod == "roll" ? "active" : "")" 
                                            @onclick='() => abilityScoreMethod = "roll"'>
                                        <i class="bi bi-dice-5 me-2"></i>Roll 4d6 Drop Lowest
                                    </button>
                                    <button class="btn btn-outline-primary @(abilityScoreMethod == "standard" ? "active" : "")" 
                                            @onclick='() => { abilityScoreMethod = "standard"; UseStandardArray(); }'>
                                        <i class="bi bi-grid-3x3 me-2"></i>Standard Array (15,14,13,12,10,8)
                                    </button>
                                    <button class="btn btn-outline-primary @(abilityScoreMethod == "manual" ? "active" : "")" 
                                            @onclick='() => abilityScoreMethod = "manual"'>
                                        <i class="bi bi-pencil me-2"></i>Manual Entry
                                    </button>
                                </div>
                            </div>

                            @if (abilityScoreMethod == "roll")
                            {
                                <div class="text-center mb-4">
                                    <button class="btn btn-lg btn-danger pulse-button" @onclick="RollAllAbilityScores">
                                        <i class="bi bi-dice-6-fill me-2"></i>Roll All Ability Scores!
                                    </button>
                                    @if (!string.IsNullOrEmpty(lastRollDetails))
                                    {
                                        <div class="alert alert-info mt-3">
                                            <small>@lastRollDetails</small>
                                        </div>
                                    }
                                </div>
                            }

                            <div class="row g-3">
                                <div class="col-md-4">
                                    <label class="form-label fw-bold text-danger">Strength (STR)</label>
                                    <div class="input-group input-group-lg">
                                        <input type="number" class="form-control" @bind="strength" min="1" max="20" />
                                        @if (abilityScoreMethod == "roll")
                                        {
                                            <button class="btn btn-outline-danger" @onclick="() => RollSingleAbility(0)">
                                                <i class="bi bi-dice-3"></i>
                                            </button>
                                        }
                                    </div>
                                    <small class="text-muted">Modifier: @CharacterStats.GetAbilityModifier(strength).ToString("+0;-#")</small>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold text-success">Dexterity (DEX)</label>
                                    <div class="input-group input-group-lg">
                                        <input type="number" class="form-control" @bind="dexterity" min="1" max="20" />
                                        @if (abilityScoreMethod == "roll")
                                        {
                                            <button class="btn btn-outline-success" @onclick="() => RollSingleAbility(1)">
                                                <i class="bi bi-dice-3"></i>
                                            </button>
                                        }
                                    </div>
                                    <small class="text-muted">Modifier: @CharacterStats.GetAbilityModifier(dexterity).ToString("+0;-#")</small>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold text-warning">Constitution (CON)</label>
                                    <div class="input-group input-group-lg">
                                        <input type="number" class="form-control" @bind="constitution" min="1" max="20" />
                                        @if (abilityScoreMethod == "roll")
                                        {
                                            <button class="btn btn-outline-warning" @onclick="() => RollSingleAbility(2)">
                                                <i class="bi bi-dice-3"></i>
                                            </button>
                                        }
                                    </div>
                                    <small class="text-muted">Modifier: @CharacterStats.GetAbilityModifier(constitution).ToString("+0;-#")</small>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold text-primary">Intelligence (INT)</label>
                                    <div class="input-group input-group-lg">
                                        <input type="number" class="form-control" @bind="intelligence" min="1" max="20" />
                                        @if (abilityScoreMethod == "roll")
                                        {
                                            <button class="btn btn-outline-primary" @onclick="() => RollSingleAbility(3)">
                                                <i class="bi bi-dice-3"></i>
                                            </button>
                                        }
                                    </div>
                                    <small class="text-muted">Modifier: @CharacterStats.GetAbilityModifier(intelligence).ToString("+0;-#")</small>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold text-info">Wisdom (WIS)</label>
                                    <div class="input-group input-group-lg">
                                        <input type="number" class="form-control" @bind="wisdom" min="1" max="20" />
                                        @if (abilityScoreMethod == "roll")
                                        {
                                            <button class="btn btn-outline-info" @onclick="() => RollSingleAbility(4)">
                                                <i class="bi bi-dice-3"></i>
                                            </button>
                                        }
                                    </div>
                                    <small class="text-muted">Modifier: @CharacterStats.GetAbilityModifier(wisdom).ToString("+0;-#")</small>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold text-secondary">Charisma (CHA)</label>
                                    <div class="input-group input-group-lg">
                                        <input type="number" class="form-control" @bind="charisma" min="1" max="20" />
                                        @if (abilityScoreMethod == "roll")
                                        {
                                            <button class="btn btn-outline-secondary" @onclick="() => RollSingleAbility(5)">
                                                <i class="bi bi-dice-3"></i>
                                            </button>
                                        }
                                    </div>
                                    <small class="text-muted">Modifier: @CharacterStats.GetAbilityModifier(charisma).ToString("+0;-#")</small>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer bg-light">
                            <button class="btn btn-secondary btn-lg" @onclick="() => SetActiveTab(3)">
                                <i class="bi bi-arrow-left me-2"></i> Back
                            </button>
                            <button class="btn btn-primary btn-lg float-end" 
                                    @onclick="() => SetActiveTab(5)">
                                Next: Skills <i class="bi bi-arrow-right ms-2"></i>
                            </button>
                        </div>
                    </div>
                }

                <!-- Tab 5: Skills -->
                @if (activeTab == 5)
                {
                    <div class="card shadow-lg border-0 animate-slide-in">
                        <div class="card-header bg-gradient-secondary text-white">
                            <h4 class="mb-0"><i class="bi bi-list-check me-2"></i>Choose Skills</h4>
                        </div>
                        <div class="card-body p-4">
                            <p class="text-muted mb-4">Select the skills your character is proficient in.</p>
                            <div class="row g-3">
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillAcrobatics" @bind="selectedSkills.Acrobatics" />
                                        <label class="form-check-label" for="skillAcrobatics">
                                            <strong>Acrobatics</strong> <small class="text-muted">(DEX)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillAnimalHandling" @bind="selectedSkills.AnimalHandling" />
                                        <label class="form-check-label" for="skillAnimalHandling">
                                            <strong>Animal Handling</strong> <small class="text-muted">(WIS)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillArcana" @bind="selectedSkills.Arcana" />
                                        <label class="form-check-label" for="skillArcana">
                                            <strong>Arcana</strong> <small class="text-muted">(INT)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillAthletics" @bind="selectedSkills.Athletics" />
                                        <label class="form-check-label" for="skillAthletics">
                                            <strong>Athletics</strong> <small class="text-muted">(STR)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillDeception" @bind="selectedSkills.Deception" />
                                        <label class="form-check-label" for="skillDeception">
                                            <strong>Deception</strong> <small class="text-muted">(CHA)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillHistory" @bind="selectedSkills.History" />
                                        <label class="form-check-label" for="skillHistory">
                                            <strong>History</strong> <small class="text-muted">(INT)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillInsight" @bind="selectedSkills.Insight" />
                                        <label class="form-check-label" for="skillInsight">
                                            <strong>Insight</strong> <small class="text-muted">(WIS)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillIntimidation" @bind="selectedSkills.Intimidation" />
                                        <label class="form-check-label" for="skillIntimidation">
                                            <strong>Intimidation</strong> <small class="text-muted">(CHA)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillInvestigation" @bind="selectedSkills.Investigation" />
                                        <label class="form-check-label" for="skillInvestigation">
                                            <strong>Investigation</strong> <small class="text-muted">(INT)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillMedicine" @bind="selectedSkills.Medicine" />
                                        <label class="form-check-label" for="skillMedicine">
                                            <strong>Medicine</strong> <small class="text-muted">(WIS)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillNature" @bind="selectedSkills.Nature" />
                                        <label class="form-check-label" for="skillNature">
                                            <strong>Nature</strong> <small class="text-muted">(INT)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillPerception" @bind="selectedSkills.Perception" />
                                        <label class="form-check-label" for="skillPerception">
                                            <strong>Perception</strong> <small class="text-muted">(WIS)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillPerformance" @bind="selectedSkills.Performance" />
                                        <label class="form-check-label" for="skillPerformance">
                                            <strong>Performance</strong> <small class="text-muted">(CHA)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillPersuasion" @bind="selectedSkills.Persuasion" />
                                        <label class="form-check-label" for="skillPersuasion">
                                            <strong>Persuasion</strong> <small class="text-muted">(CHA)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillReligion" @bind="selectedSkills.Religion" />
                                        <label class="form-check-label" for="skillReligion">
                                            <strong>Religion</strong> <small class="text-muted">(INT)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillSleightOfHand" @bind="selectedSkills.SleightOfHand" />
                                        <label class="form-check-label" for="skillSleightOfHand">
                                            <strong>Sleight of Hand</strong> <small class="text-muted">(DEX)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillStealth" @bind="selectedSkills.Stealth" />
                                        <label class="form-check-label" for="skillStealth">
                                            <strong>Stealth</strong> <small class="text-muted">(DEX)</small>
                                        </label>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-check form-check-lg">
                                        <input class="form-check-input" type="checkbox" id="skillSurvival" @bind="selectedSkills.Survival" />
                                        <label class="form-check-label" for="skillSurvival">
                                            <strong>Survival</strong> <small class="text-muted">(WIS)</small>
                                        </label>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer bg-light">
                            <button class="btn btn-secondary btn-lg" @onclick="() => SetActiveTab(4)">
                                <i class="bi bi-arrow-left me-2"></i> Back
                            </button>
                            <button class="btn btn-primary btn-lg float-end" 
                                    @onclick="() => SetActiveTab(6)">
                                Next: Feats <i class="bi bi-arrow-right ms-2"></i>
                            </button>
                        </div>
                    </div>
                }

                <!-- Tab 6: Feats -->
                @if (activeTab == 6)
                {
                    <div class="card shadow-lg border-0 animate-slide-in">
                        <div class="card-header bg-gradient-secondary text-white">
                            <h4 class="mb-0"><i class="bi bi-star-fill me-2"></i>Choose Feats (Optional)</h4>
                        </div>
                        <div class="card-body p-4">
                            @if (availableFeats == null)
                            {
                                <div class="text-center py-3">
                                    <div class="spinner-border text-primary" role="status">
                                        <span class="visually-hidden">Loading...</span>
                                    </div>
                                </div>
                            }
                            else
                            {
                                <div class="row g-3 mb-4">
                                    @foreach (var feat in availableFeats)
                                    {
                                        <div class="col-md-6">
                                            <div class="card hover-card @(selectedFeats.Contains(feat) ? "border-primary border-2" : "")" 
                                                 @onclick="() => ToggleFeat(feat)"
                                                 style="cursor: pointer;">
                                                <div class="card-body">
                                                    <h6 class="card-title">
                                                        @feat.Name
                                                        @if (selectedFeats.Contains(feat))
                                                        {
                                                            <span class="badge bg-primary float-end">
                                                                <i class="bi bi-check"></i>
                                                            </span>
                                                        }
                                                    </h6>
                                                    <p class="card-text small text-muted">@feat.Description</p>
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>

                                @if (selectedFeats.Any())
                                {
                                    <div class="alert alert-primary">
                                        <strong>Selected Feats (@selectedFeats.Count):</strong>
                                        @string.Join(", ", selectedFeats.Select(f => f.Name))
                                    </div>
                                }
                            }
                        </div>
                        <div class="card-footer bg-light">
                            <button class="btn btn-secondary btn-lg" @onclick="() => SetActiveTab(5)">
                                <i class="bi bi-arrow-left me-2"></i> Back
                            </button>
                            <button class="btn btn-primary btn-lg float-end" 
                                    @onclick="() => SetActiveTab(7)">
                                Next: Spells <i class="bi bi-arrow-right ms-2"></i>
                            </button>
                        </div>
                    </div>
                }

                <!-- Tab 7: Spells -->
                @if (activeTab == 7)
                {
                    <div class="card shadow-lg border-0 animate-slide-in">
                        <div class="card-header bg-gradient-primary text-white">
                            <h4 class="mb-0"><i class="bi bi-magic me-2"></i>Choose Spells (Optional)</h4>
                        </div>
                        <div class="card-body p-4">
                            @if (availableSpells == null)
                            {
                                <div class="text-center py-3">
                                    <div class="spinner-border text-primary" role="status">
                                        <span class="visually-hidden">Loading...</span>
                                    </div>
                                    <p class="mt-3 text-muted">Loading spells...</p>
                                </div>
                            }
                            else if (!availableSpells.Any())
                            {
                                <div class="alert alert-info">
                                    <i class="bi bi-info-circle me-2"></i>
                                    No spells available. This might be because your class doesn't have spellcasting abilities.
                                </div>
                            }
                            else
                            {
                                <div class="mb-4">
                                    <label class="form-label fw-bold">Filter by Level</label>
                                    <select class="form-select" @bind="selectedSpellLevel">
                                        <option value="-1">All Levels</option>
                                        <option value="0">Cantrips</option>
                                        <option value="1">1st Level</option>
                                        <option value="2">2nd Level</option>
                                        <option value="3">3rd Level</option>
                                        <option value="4">4th Level</option>
                                        <option value="5">5th Level</option>
                                        <option value="6">6th Level</option>
                                        <option value="7">7th Level</option>
                                        <option value="8">8th Level</option>
                                        <option value="9">9th Level</option>
                                    </select>
                                </div>

                                <div class="row g-3 mb-4">
                                    @foreach (var spell in GetFilteredSpells())
                                    {
                                        <div class="col-md-6">
                                            <div class="card hover-card @(selectedSpells.Contains(spell) ? "border-primary border-2" : "")" 
                                                 @onclick="() => ToggleSpell(spell)"
                                                 style="cursor: pointer;">
                                                <div class="card-body">
                                                    <h6 class="card-title">
                                                        @spell.Name
                                                        @if (selectedSpells.Contains(spell))
                                                        {
                                                            <span class="badge bg-primary float-end">
                                                                <i class="bi bi-check"></i>
                                                            </span>
                                                        }
                                                    </h6>
                                                    <small class="text-muted d-block">
                                                        <strong>Level:</strong> @(spell.Level == 0 ? "Cantrip" : spell.Level.ToString())
                                                        | <strong>School:</strong> @spell.School
                                                    </small>
                                                    <p class="card-text small text-muted mt-2">@spell.Description</p>
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>

                                @if (selectedSpells.Any())
                                {
                                    <div class="alert alert-primary">
                                        <strong>Selected Spells (@selectedSpells.Count):</strong>
                                        @string.Join(", ", selectedSpells.Select(s => s.Name))
                                    </div>
                                }
                            }
                        </div>
                        <div class="card-footer bg-light">
                            <button class="btn btn-secondary btn-lg" @onclick="() => SetActiveTab(6)">
                                <i class="bi bi-arrow-left me-2"></i> Back
                            </button>
                            <button class="btn btn-primary btn-lg float-end" 
                                    @onclick="() => SetActiveTab(8)">
                                Next: Equipment <i class="bi bi-arrow-right ms-2"></i>
                            </button>
                        </div>
                    </div>
                }

                <!-- Tab 8: Equipment -->
                @if (activeTab == 8)
                {
                    <div class="card shadow-lg border-0 animate-slide-in">
                        <div class="card-header bg-gradient-warning text-dark">
                            <h4 class="mb-0"><i class="bi bi-backpack me-2"></i>Choose Equipment (Optional)</h4>
                        </div>
                        <div class="card-body p-4">
                            <ul class="nav nav-tabs mb-4" role="tablist">
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link @(equipmentTab == 0 ? "active" : "")" 
                                            @onclick="() => equipmentTab = 0" 
                                            type="button">
                                        <i class="bi bi-shield me-1"></i> Armor
                                    </button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link @(equipmentTab == 1 ? "active" : "")" 
                                            @onclick="() => equipmentTab = 1" 
                                            type="button">
                                        <i class="bi bi-x-diamond me-1"></i> Weapons
                                    </button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link @(equipmentTab == 2 ? "active" : "")" 
                                            @onclick="() => equipmentTab = 2" 
                                            type="button">
                                        <i class="bi bi-bag me-1"></i> Adventuring Gear
                                    </button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link @(equipmentTab == 3 ? "active" : "")" 
                                            @onclick="() => equipmentTab = 3" 
                                            type="button">
                                        <i class="bi bi-wrench me-1"></i> Tools
                                    </button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link @(equipmentTab == 4 ? "active" : "")" 
                                            @onclick="() => equipmentTab = 4" 
                                            type="button">
                                        <i class="bi bi-coin me-1"></i> Wealth
                                    </button>
                                </li>
                            </ul>

                            <!-- Armor Sub-tab -->
                            @if (equipmentTab == 0)
                            {
                                @if (availableArmor == null)
                                {
                                    <div class="text-center py-3">
                                        <div class="spinner-border text-primary" role="status"></div>
                                    </div>
                                }
                                else
                                {
                                    <div class="row g-3">
                                        @foreach (var armor in availableArmor)
                                        {
                                            <div class="col-md-6">
                                                <div class="card hover-card @(selectedArmor.Contains(armor) ? "border-warning border-2" : "")" 
                                                     @onclick="() => ToggleArmor(armor)"
                                                     style="cursor: pointer;">
                                                    <div class="card-body">
                                                        <h6 class="card-title">
                                                            @armor.Name
                                                            @if (selectedArmor.Contains(armor))
                                                            {
                                                                <span class="badge bg-warning text-dark float-end">
                                                                    <i class="bi bi-check"></i>
                                                                </span>
                                                            }
                                                        </h6>
                                                        <small class="text-muted">
                                                            <div><strong>AC:</strong> @armor.ArmorClass</div>
                                                            <div><strong>Type:</strong> @armor.Category</div>
                                                            @if (!string.IsNullOrEmpty(armor.Cost))
                                                            {
                                                                <div><strong>Cost:</strong> @armor.Cost</div>
                                                            }
                                                        </small>
                                                    </div>
                                                </div>
                                            </div>
                                        }
                                    </div>
                                }
                            }

                            <!-- Weapons Sub-tab -->
                            @if (equipmentTab == 1)
                            {
                                @if (availableWeapons == null)
                                {
                                    <div class="text-center py-3">
                                        <div class="spinner-border text-primary" role="status"></div>
                                    </div>
                                }
                                else
                                {
                                    <div class="row g-3">
                                        @foreach (var weapon in availableWeapons)
                                        {
                                            <div class="col-md-6">
                                                <div class="card hover-card @(selectedWeapons.Contains(weapon) ? "border-warning border-2" : "")" 
                                                     @onclick="() => ToggleWeapon(weapon)"
                                                     style="cursor: pointer;">
                                                    <div class="card-body">
                                                        <h6 class="card-title">
                                                            @weapon.Name
                                                            @if (selectedWeapons.Contains(weapon))
                                                            {
                                                                <span class="badge bg-warning text-dark float-end">
                                                                    <i class="bi bi-check"></i>
                                                                </span>
                                                            }
                                                        </h6>
                                                        <small class="text-muted">
                                                            <div><strong>Damage:</strong> @weapon.Damage</div>
                                                            <div><strong>Type:</strong> @weapon.Type</div>
                                                            @if (!string.IsNullOrEmpty(weapon.Cost))
                                                            {
                                                                <div><strong>Cost:</strong> @weapon.Cost</div>
                                                            }
                                                        </small>
                                                    </div>
                                                </div>
                                            </div>
                                        }
                                    </div>
                                }
                            }

                            <!-- Adventuring Gear Sub-tab -->
                            @if (equipmentTab == 2)
                            {
                                @if (availableAdventuringGear == null)
                                {
                                    <div class="text-center py-3">
                                        <div class="spinner-border text-primary" role="status"></div>
                                    </div>
                                }
                                else
                                {
                                    <div class="row g-3">
                                        @foreach (var gear in availableAdventuringGear)
                                        {
                                            <div class="col-md-6">
                                                <div class="card hover-card @(selectedAdventuringGear.Contains(gear) ? "border-warning border-2" : "")" 
                                                     @onclick="() => ToggleAdventuringGear(gear)"
                                                     style="cursor: pointer;">
                                                    <div class="card-body">
                                                        <h6 class="card-title">
                                                            @gear.Name
                                                            @if (selectedAdventuringGear.Contains(gear))
                                                            {
                                                                <span class="badge bg-warning text-dark float-end">
                                                                    <i class="bi bi-check"></i>
                                                                </span>
                                                            }
                                                        </h6>
                                                        <small class="text-muted">
                                                            @if (!string.IsNullOrEmpty(gear.Cost))
                                                            {
                                                                <div><strong>Cost:</strong> @gear.Cost</div>
                                                            }
                                                            @if (!string.IsNullOrEmpty(gear.Weight))
                                                            {
                                                                <div><strong>Weight:</strong> @gear.Weight</div>
                                                            }
                                                        </small>
                                                    </div>
                                                </div>
                                            </div>
                                        }
                                    </div>
                                }
                            }

                            <!-- Tools Sub-tab -->
                            @if (equipmentTab == 3)
                            {
                                @if (availableTools == null)
                                {
                                    <div class="text-center py-3">
                                        <div class="spinner-border text-primary" role="status"></div>
                                    </div>
                                }
                                else
                                {
                                    <div class="row g-3">
                                        @foreach (var tool in availableTools)
                                        {
                                            <div class="col-md-6">
                                                <div class="card hover-card @(selectedTools.Contains(tool) ? "border-warning border-2" : "")" 
                                                     @onclick="() => ToggleTool(tool)"
                                                     style="cursor: pointer;">
                                                    <div class="card-body">
                                                        <h6 class="card-title">
                                                            @tool.Name
                                                            @if (selectedTools.Contains(tool))
                                                            {
                                                                <span class="badge bg-warning text-dark float-end">
                                                                    <i class="bi bi-check"></i>
                                                                </span>
                                                            }
                                                        </h6>
                                                        <small class="text-muted">
                                                            <div><strong>Type:</strong> @tool.Category</div>
                                                            @if (!string.IsNullOrEmpty(tool.Cost))
                                                            {
                                                                <div><strong>Cost:</strong> @tool.Cost</div>
                                                            }
                                                        </small>
                                                    </div>
                                                </div>
                                            </div>
                                        }
                                    </div>
                                }
                            }

                            <!-- Wealth Sub-tab -->
                            @if (equipmentTab == 4)
                            {
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold"><i class="bi bi-coin text-warning"></i> Copper Pieces (CP)</label>
                                        <input type="number" class="form-control" @bind="characterWealth.CopperPieces" min="0" />
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold"><i class="bi bi-coin text-secondary"></i> Silver Pieces (SP)</label>
                                        <input type="number" class="form-control" @bind="characterWealth.SilverPieces" min="0" />
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold"><i class="bi bi-coin text-info"></i> Electrum Pieces (EP)</label>
                                        <input type="number" class="form-control" @bind="characterWealth.ElectrumPieces" min="0" />
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold"><i class="bi bi-coin text-warning"></i> Gold Pieces (GP)</label>
                                        <input type="number" class="form-control" @bind="characterWealth.GoldPieces" min="0" />
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label fw-bold"><i class="bi bi-coin text-light"></i> Platinum Pieces (PP)</label>
                                        <input type="number" class="form-control" @bind="characterWealth.PlatinumPieces" min="0" />
                                    </div>
                                </div>
                            }
                        </div>
                        <div class="card-footer bg-light">
                            <button class="btn btn-secondary btn-lg" @onclick="() => SetActiveTab(7)">
                                <i class="bi bi-arrow-left me-2"></i> Back
                            </button>
                            <button class="btn btn-success btn-lg float-end" 
                                    @onclick="() => { BuildCharacter(); SetActiveTab(9); }">
                                Complete & Preview <i class="bi bi-check-circle ms-2"></i>
                            </button>
                        </div>
                    </div>
                }

                <!-- Tab 9: Preview & Export -->
                @if (activeTab == 9)
                {
                    <div class="animate-slide-in">
                        @if (builtCharacter != null)
                        {
                            <div class="card shadow-lg border-0 mb-4">
                                <div class="card-header bg-gradient-success text-white">
                                    <h4 class="mb-0">
                                        <i class="bi bi-check-circle-fill me-2"></i>
                                        Character Complete: @builtCharacter.Information?.Name
                                    </h4>
                                </div>
                                <div class="card-body">
                                    @if (!string.IsNullOrEmpty(saveMessage))
                                    {
                                        <div class="alert alert-success alert-dismissible fade show">
                                            <i class="bi bi-check-circle me-2"></i>
                                            @saveMessage
                                            <button type="button" class="btn-close" @onclick="() => saveMessage = string.Empty"></button>
                                        </div>
                                    }

                                    <div class="d-flex gap-2 mb-4 flex-wrap">
                                        <button class="btn btn-success btn-lg" @onclick="SaveCharacter" disabled="@isSaving">
                                            @if (isSaving)
                                            {
                                                <span class="spinner-border spinner-border-sm me-2"></span>
                                                <span>Saving...</span>
                                            }
                                            else
                                            {
                                                <i class="bi bi-floppy me-2"></i>
                                                <span>Save Character</span>
                                            }
                                        </button>
                                        <button class="btn btn-primary btn-lg" @onclick="GeneratePDF" disabled="@isGeneratingPDF">
                                            @if (isGeneratingPDF)
                                            {
                                                <span class="spinner-border spinner-border-sm me-2"></span>
                                                <span>Generating...</span>
                                            }
                                            else
                                            {
                                                <i class="bi bi-file-pdf me-2"></i>
                                                <span>Generate PDF</span>
                                            }
                                        </button>
                                        <button class="btn btn-info btn-lg" @onclick="DownloadPDF" disabled="@string.IsNullOrEmpty(pdfDataUrl)">
                                            <i class="bi bi-download me-2"></i> Download PDF
                                        </button>
                                        <button class="btn btn-outline-secondary btn-lg" @onclick="ViewMyCharacters">
                                            <i class="bi bi-people me-2"></i> My Characters
                                        </button>
                                        <button class="btn btn-outline-warning btn-lg" @onclick="ResetBuilder">
                                            <i class="bi bi-arrow-clockwise me-2"></i> Start Over
                                        </button>
                                    </div>

                                    @if (!string.IsNullOrEmpty(pdfDataUrl))
                                    {
                                        <div class="mb-4">
                                            <iframe src="@pdfDataUrl" style="width: 100%; height: 600px; border: 1px solid #ddd; border-radius: 8px;"></iframe>
                                        </div>
                                    }
                                </div>
                            </div>

                            <CharacterSheetDisplay Character="builtCharacter" />
                        }
                        else
                        {
                            <div class="alert alert-warning">
                                <i class="bi bi-exclamation-triangle me-2"></i>
                                Please complete all previous steps to build your character.
                            </div>
                        }
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<style>
    .character-tabs .nav-link {
        padding: 1rem;
        transition: all 0.3s ease;
        border-radius: 0.5rem;
        margin: 0 0.25rem;
    }
    
    .character-tabs .nav-link:not(.active):hover {
        background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
        transform: translateY(-2px);
    }
    
    .character-tabs .nav-link.active {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        transform: translateY(-3px);
        box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
    }
    
    .bg-gradient-primary { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); }
    .bg-gradient-success { background: linear-gradient(135deg, #52c234 0%, #061700 100%); }
    .bg-gradient-warning { background: linear-gradient(135deg, #FFD93D 0%, #FF8C38 100%); }
    .bg-gradient-info { background: linear-gradient(135deg, #0093E9 0%, #80D0C7 100%); }
    .bg-gradient-danger { background: linear-gradient(135deg, #ff6b6b 0%, #c92a2a 100%); }
    .bg-gradient-secondary { background: linear-gradient(135deg, #6c757d 0%, #343a40 100%); }
    
    .hover-card {
        transition: all 0.3s ease;
        cursor: pointer;
    }
    
    .hover-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 25px rgba(0,0,0,0.15);
    }
    
    .animate-slide-in {
        animation: slideIn 0.4s ease-out;
    }
    
    @@keyframes slideIn {
        from {
            opacity: 0;
            transform: translateX(20px);
        }
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }
    
    .pulse-button {
        animation: pulse 2s infinite;
    }
    
    @@keyframes pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.05); }
    }
    
    input[type="number"]::-webkit-inner-spin-button,
    input[type="number"]::-webkit-outer-spin-button {
        opacity: 1;
    }
</style>

@code {
    private int activeTab = 0;
    private Character? builtCharacter;
    private string pdfDataUrl = string.Empty;
    private bool isGeneratingPDF = false;
    private bool isSaving = false;
    private string saveMessage = string.Empty;

    // Tab 0: Basic Info
    private string characterName = "";
    private CharacterAlignment selectedAlignment = CharacterAlignment.NeutralGood;
    private string characterDescription = "";
    private int characterLevel = 1;
    private int characterExperience = 0;
    private string characterPortrait = "";

    // Tab 1: Race
    private List<DndShared.Models.Species>? availableSpecies;
    private DndShared.Models.Species? selectedSpecies;

    // Tab 2: Class
    private List<CharacterClass>? availableClasses;
    private CharacterClass? selectedClass;

    // Tab 3: Background
    private List<Background>? availableBackgrounds;
    private Background? selectedBackground;

    // Tab 4: Ability Scores
    private string abilityScoreMethod = "roll";
    private int strength = 10;
    private int dexterity = 10;
    private int constitution = 10;
    private int intelligence = 10;
    private int wisdom = 10;
    private int charisma = 10;
    private string lastRollDetails = "";

    // Tab 5: Skills
    private CharacterSkills selectedSkills = new();

    // Tab 6: Feats
    private List<Feat>? availableFeats;
    private List<Feat> selectedFeats = new();

    // Tab 7: Spells
    private List<Spell>? availableSpells;
    private List<Spell> selectedSpells = new();
    private int selectedSpellLevel = -1;

    // Tab 8: Equipment
    private int equipmentTab = 0;
    private List<DndShared.Models.Armor>? availableArmor;
    private List<DndShared.Models.Armor> selectedArmor = new();
    private List<DndShared.Models.Weapon>? availableWeapons;
    private List<DndShared.Models.Weapon> selectedWeapons = new();
    private List<DndShared.Models.AdventuringGear>? availableAdventuringGear;
    private List<DndShared.Models.AdventuringGear> selectedAdventuringGear = new();
    private List<DndShared.Models.Tool>? availableTools;
    private List<DndShared.Models.Tool> selectedTools = new();
    private CharacterWealth characterWealth = new();

    protected override async Task OnInitializedAsync()
    {
        await LoadAllData();
    }

    private async Task LoadAllData()
    {
        try
        {
            availableSpecies = await SpeciesService.GetAllSpeciesAsync();
            availableClasses = await ClassService.GetAllClassesAsync();
            availableBackgrounds = await BackgroundService.GetAllBackgroundsAsync();
            availableFeats = await FeatService.GetAllFeatsAsync();
            availableSpells = await SpellService.GetAllSpellsAsync();
            availableArmor = await ArmorService.GetAllArmorAsync();
            availableWeapons = await WeaponService.GetAllWeaponsAsync();
            availableAdventuringGear = await AdventuringGearService.GetAllAdventuringGearAsync();
            availableTools = await ToolService.GetAllToolsAsync();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading data: {ex.Message}");
        }
    }

    private void SetActiveTab(int tabIndex)
    {
        activeTab = tabIndex;
        StateHasChanged();
    }

    private async Task HandlePortraitUpload(InputFileChangeEventArgs e)
    {
        var file = e.File;
        if (file != null)
        {
            try
            {
                var buffer = new byte[file.Size];
                await file.OpenReadStream(maxAllowedSize: 5 * 1024 * 1024).ReadAsync(buffer);
                var imageType = file.ContentType;
                characterPortrait = $"data:{imageType};base64,{Convert.ToBase64String(buffer)}";
                StateHasChanged();
            }
            catch (Exception ex)
            {
                Console.WriteLine($"Error uploading portrait: {ex.Message}");
            }
        }
    }

    private void ClearPortrait()
    {
        characterPortrait = "";
        StateHasChanged();
    }

    private void SelectSpecies(DndShared.Models.Species species)
    {
        selectedSpecies = species;
        StateHasChanged();
    }

    private void SelectClass(CharacterClass classOption)
    {
        selectedClass = classOption;
        StateHasChanged();
    }

    private void SelectBackground(Background background)
    {
        selectedBackground = background;
        StateHasChanged();
    }

    private void ToggleFeat(Feat feat)
    {
        if (selectedFeats.Contains(feat))
            selectedFeats.Remove(feat);
        else
            selectedFeats.Add(feat);
        StateHasChanged();
    }

    private void ToggleSpell(Spell spell)
    {
        if (selectedSpells.Contains(spell))
            selectedSpells.Remove(spell);
        else
            selectedSpells.Add(spell);
        StateHasChanged();
    }

    private List<Spell> GetFilteredSpells()
    {
        if (availableSpells == null) return new List<Spell>();
        
        if (selectedSpellLevel == -1)
            return availableSpells;
        
        return availableSpells.Where(s => s.Level == selectedSpellLevel).ToList();
    }

    private void ToggleArmor(DndShared.Models.Armor armor)
    {
        if (selectedArmor.Contains(armor))
            selectedArmor.Remove(armor);
        else
            selectedArmor.Add(armor);
        StateHasChanged();
    }

    private void ToggleWeapon(DndShared.Models.Weapon weapon)
    {
        if (selectedWeapons.Contains(weapon))
            selectedWeapons.Remove(weapon);
        else
            selectedWeapons.Add(weapon);
        StateHasChanged();
    }

    private void ToggleAdventuringGear(DndShared.Models.AdventuringGear gear)
    {
        if (selectedAdventuringGear.Contains(gear))
            selectedAdventuringGear.Remove(gear);
        else
            selectedAdventuringGear.Add(gear);
        StateHasChanged();
    }

    private void ToggleTool(DndShared.Models.Tool tool)
    {
        if (selectedTools.Contains(tool))
            selectedTools.Remove(tool);
        else
            selectedTools.Add(tool);
        StateHasChanged();
    }

    private async Task RollAllAbilityScores()
    {
        var rolls = new List<string>();
        
        strength = await RollAbilityScore();
        rolls.Add($"STR: {strength}");
        
        dexterity = await RollAbilityScore();
        rolls.Add($"DEX: {dexterity}");
        
        constitution = await RollAbilityScore();
        rolls.Add($"CON: {constitution}");
        
        intelligence = await RollAbilityScore();
        rolls.Add($"INT: {intelligence}");
        
        wisdom = await RollAbilityScore();
        rolls.Add($"WIS: {wisdom}");
        
        charisma = await RollAbilityScore();
        rolls.Add($"CHA: {charisma}");

        lastRollDetails = $"Rolled: {string.Join(", ", rolls)}";
        StateHasChanged();
    }

    private async Task RollSingleAbility(int abilityIndex)
    {
        int roll = await RollAbilityScore();
        
        switch (abilityIndex)
        {
            case 0: strength = roll; break;
            case 1: dexterity = roll; break;
            case 2: constitution = roll; break;
            case 3: intelligence = roll; break;
            case 4: wisdom = roll; break;
            case 5: charisma = roll; break;
        }
        
        StateHasChanged();
    }

    private async Task<int> RollAbilityScore()
    {
        // Roll 4d6, drop lowest
        var rolls = new List<int>();
        for (int i = 0; i < 4; i++)
        {
            rolls.Add(DiceService.RollDice(6));
            await Task.Delay(1); // Small delay for animation effect
        }
        rolls.Sort();
        rolls.RemoveAt(0); // Remove lowest
        return rolls.Sum();
    }

    private void UseStandardArray()
    {
        var standardArray = new[] { 15, 14, 13, 12, 10, 8 };
        strength = standardArray[0];
        dexterity = standardArray[1];
        constitution = standardArray[2];
        intelligence = standardArray[3];
        wisdom = standardArray[4];
        charisma = standardArray[5];
        StateHasChanged();
    }

    private void BuildCharacter()
    {
        builtCharacter = new Character
        {
            Information = new CharacterInformation
            {
                Name = characterName,
                Race = selectedSpecies?.Name ?? "Unknown",
                Class = selectedClass?.Name ?? "Unknown",
                Background = selectedBackground?.Name ?? "Unknown",
                Alignment = selectedAlignment,
                BaseInformation = new ChraracterSheetInformation
                {
                    Level = characterLevel,
                    ExperiencePoints = characterExperience,
                    CharacterPortrait = characterPortrait,
                    Skills = selectedSkills,
                    Spellcasting = selectedSpells.Any() ? new CharacterSpellcasting
                    {
                        Spells = selectedSpells
                    } : null,
                    Equipment = new CharacterEquipment
                    {
                        Armors = selectedArmor,
                        Weapons = selectedWeapons,
                        AdventuringGears = selectedAdventuringGear,
                        Tools = selectedTools,
                        Wealth = characterWealth
                    }
                }
            },
            Stats = new CharacterStats
            {
                Strength = strength,
                Dexterity = dexterity,
                Constitution = constitution,
                Intelligence = intelligence,
                Wisdom = wisdom,
                Charisma = charisma
            },
            Race = selectedSpecies,
            Class = selectedClass,
            Background = selectedBackground,
            Feats = selectedFeats
        };
    }

    private async Task GeneratePDF()
    {
        if (builtCharacter == null) return;
        
        isGeneratingPDF = true;
        StateHasChanged();
        
        try
        {
            pdfDataUrl = await CharacterSheetService.FillCharacterSheetAsync(builtCharacter, "2024");
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error generating PDF: {ex.Message}");
        }
        finally
        {
            isGeneratingPDF = false;
            StateHasChanged();
        }
    }

    private async Task DownloadPDF()
    {
        if (builtCharacter == null) return;
        
        try
        {
            await CharacterSheetService.DownloadCharacterSheetAsync(
                builtCharacter,
                $"{builtCharacter.Information?.Name ?? "Character"}.pdf",
                "2024"
            );
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error downloading PDF: {ex.Message}");
        }
    }

    private async Task SaveCharacter()
    {
        if (builtCharacter == null) return;

        isSaving = true;
        saveMessage = string.Empty;
        StateHasChanged();

        try
        {
            await StorageService.SaveCharacterAsync(builtCharacter);
            saveMessage = $"✓ '{builtCharacter.Information?.Name}' saved successfully!";
        }
        catch (Exception ex)
        {
            saveMessage = $"✗ Error saving character: {ex.Message}";
            Console.WriteLine($"Error saving character: {ex.Message}");
        }
        finally
        {
            isSaving = false;
            StateHasChanged();
        }
    }

    private void ViewMyCharacters()
    {
        NavigationManager.NavigateTo("/mycharacters");
    }

    private void ResetBuilder()
    {
        activeTab = 0;
        builtCharacter = null;
        pdfDataUrl = string.Empty;
        characterName = "";
        selectedAlignment = CharacterAlignment.NeutralGood;
        characterDescription = "";
        characterLevel = 1;
        characterExperience = 0;
        characterPortrait = "";
        selectedSpecies = null;
        selectedClass = null;
        selectedBackground = null;
        abilityScoreMethod = "roll";
        strength = dexterity = constitution = intelligence = wisdom = charisma = 10;
        selectedSkills = new CharacterSkills();
        selectedFeats.Clear();
        selectedSpells.Clear();
        selectedSpellLevel = -1;
        equipmentTab = 0;
        selectedArmor.Clear();
        selectedWeapons.Clear();
        selectedAdventuringGear.Clear();
        selectedTools.Clear();
        characterWealth = new CharacterWealth();
        lastRollDetails = "";
        saveMessage = string.Empty;
        StateHasChanged();
    }
}
